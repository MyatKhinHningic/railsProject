<% if user_signed_in? %>
<h1>Friend List</h1>
<table class = "table table-striped table-bordered table-hover">
  <thead class = "table-dark">
      <tr>
          <th>Name</th>
          <th>Email</th>
          <th>Phone</th>
          <th>Twitter</th>
          <th></th>
      </tr>
  </thead>

   <tbody>
    <% @friends.each do |friend| %>

    <% if friend.user == current_user %>
      <tr>
          <td>
          <%= link_to  friend.first_name + ' ' + friend.last_name, friend %>
          </td>
          
          <td><%= friend.email %></td>
          <td><%= friend.phone %></td>
          <td><%= friend.twitter %></td>
        
          <td><%= link_to "Delete", friend, data: { "turbo-method": :delete, turbo_confirm: 'Are you sure?' }, class: "btn btn-sm btn-outline-danger" %></td>
            
        </tr>
      <% end %>
   <% end %>
  </tbody>
</table>

<%= link_to "New friend", new_friend_path,class: "btn btn-secondary" %>
<% else %>

<h1>Hello World</h1>

This is my first Rails Web.


<% end %>

<%
=begin%>
 <div id="friends">
  <% @friends.each do |friend| %>
    <%= render friend %>
    <p>
      <%= link_to "Show this friend", friend %>
    </p>
  <% end %>
</div>
<%
=end%>


